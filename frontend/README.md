# 前端（Next.js 15）

- **技术**：Next.js App Router、TypeScript、Tailwind、React Query、react-hook-form、zod。
- **风格**：简洁暖色卡片，支持 `class="dark"` 暗色（右上角切换）。

## 命令

```bash
npm install
echo 'NEXT_PUBLIC_API_URL=http://localhost:8000' > .env.local
npm run dev
```

默认登录账号见项目根目录 `README.md` / 后端 `seed`。
